# The port the browser suites serve on.
PORT=4002

# Emptied by every run. Any name, any server — the reset refuses only the one .env names.
DB_HOST=127.0.0.1
DB_PORT=3306
DB_DATABASE=lineage_remastered_test
DB_USERNAME=root
DB_PASSWORD=pass
