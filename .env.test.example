# The port the browser suites serve on.
PORT=4002

# Emptied by every run, so the name must end in _test.
DB_HOST=127.0.0.1
DB_PORT=3306
DB_DATABASE=lineage_remastered_test
DB_USERNAME=root
DB_PASSWORD=pass
